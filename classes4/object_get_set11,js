const user = {
    _email: "ansh985sharma@gmail.com",
    pass: "abc123",
  
    get email() {
      return this._email.toUpperCase(); 
    },
    set email(newEmail) {
      this._email = newEmail; 
    }
  }

  const a= Object.create(user) //factory function
  console.log(a.email);
  
